# Data

The raw UCI archive is downloaded on demand to `data/raw/uci_gas_sensor_drift_270.zip` and extracted under `data/raw/uci_gas_sensor_drift_270/`.

Raw files are ignored by Git. The reproducibility manifest `data/data_manifest.json` is generated by the scripts and records the archive hash, batch file hashes, row counts, feature count and gas mapping.
